extends layout

block content
  div(class='list-title')
    h1= title

  ul
    each series in series_list
      li
        a(href=series.url) #{series.series_name}

    else
      li There are no grades at the moment.